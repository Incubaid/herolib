# ${args.classname} Kubernetes Deployment
# Generated by HeroLib Generator
#
# Apply with: kubectl apply -f deployment.yaml

apiVersion: v1
kind: Namespace
metadata:
  name: ??{k8app.namespace}
  labels:
    app: ${args.name}
    managed-by: herolib

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${args.name}
  namespace: ??{k8app.namespace}
  labels:
    app: ${args.name}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${args.name}
  template:
    metadata:
      labels:
        app: ${args.name}
    spec:
      containers:
        - name: ${args.name}
          image: ${args.name}:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: ${args.name}
  namespace: ??{k8app.namespace}
  labels:
    app: ${args.name}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: ${args.name}

