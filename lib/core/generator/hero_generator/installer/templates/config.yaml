# ${args.classname} Configuration
# Generated by HeroLib Generator

service:
  name: ??{cfg.name}
  title: "??{cfg.title}"
  host: ??{cfg.host}
  port: ??{cfg.port}

paths:
  home: ??{cfg.homedir}
  config: ??{cfg.configpath}
  data: ??{cfg.homedir}/data
  logs: ??{cfg.homedir}/logs

auth:
  username: ??{cfg.username}
  # password is managed securely via herolib secrets

logging:
  level: info
  format: json
  output: file

options:
  debug: false
  max_connections: 100
  timeout_seconds: 30

