apiVersion: v1
kind: Service
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
  labels:
    app: {{.AppLabel}}
spec:
  selector:
    app: {{.AppLabel}}
  ports:
    - protocol: TCP
      port: {{.Port}}
      targetPort: {{.TargetPort}}
  type: {{.ServiceType}}