apiVersion: v1
kind: Service
metadata:
  name: ${metadata.name}
  namespace: ${metadata.namespace}
spec:
  type: ${service_type}
  selector:
    app: ${metadata.name}
  ports:
  - protocol: TCP
    port: ${port}
    targetPort: ${target_port}